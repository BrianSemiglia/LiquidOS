The component under test: a long-running service (service.js) and static
marker content (MARKER_BEFORE, INITIAL) inlined in component.html. Its
view stays inert; on a relationship signal it rewrites its own service.js,
triggering the harness's run-mode liquidos-file to restart the service.
