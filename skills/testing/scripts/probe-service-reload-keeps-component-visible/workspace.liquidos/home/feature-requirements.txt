Fixture for the service-restart probe. A `target` component under test
(inert view + run-mode service) and a `controls` component (button +
BOOT readout), wired by a relationship: clicking the button rewrites
target/service.js, which restarts the service; the service bumps the
BOOT counter shown in controls.
